# Clone github issues from x repositories into another one

Just set the variables and run the script:

```bash
bash script.sh
```

> [!NOTE]
> Make sure you are logged in with the [GH CLI](https://cli.github.com/).
